[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "WINDIGO"
version = "0.0.1"
dependencies = []
authors = [
    {name = "Jacob Wisienski", email = "jacobwisienski86@gmail.com"}
]